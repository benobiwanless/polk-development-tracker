const cityName=document.body.dataset.city;
fetch("../../data/projects.json").then(r=>r.json()).then(projects=>{const list=projects.filter(p=>p.city===cityName);document.getElementById("cityTitle").textContent=cityName;document.getElementById("cityCount").textContent=`${list.length} tracked project${list.length===1?"":"s"}`;document.getElementById("cityProjects").innerHTML=list.map(s=>`<article class="card"><span class="tag">${s.category}</span><h3>${s.title}</h3><p class="meta">${s.status} • ${s.phase}</p><p>${s.summary}</p><a class="read-more" href="../project.html?id=${s.id}">View project →</a></article>`).join("") || `<p>No projects added yet.</p>`;});
