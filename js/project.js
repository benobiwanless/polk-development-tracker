const params=new URLSearchParams(window.location.search);const id=params.get("id");
fetch("../data/stories.json").then(r=>r.json()).then(stories=>{const s=stories.find(x=>x.id===id)||stories[0];document.title=s.title+" | Polk Development Tracker";document.getElementById("projectDetail").innerHTML=`<p class="eyebrow">${s.category}</p><h1>${s.title}</h1><p class="meta">📍 ${s.city} • Status: ${s.status} • Updated ${s.updated}</p><p>${s.summary}</p><p>${s.details}</p><p><a class="source" href="${s.sourceUrl}" target="_blank">Original source: ${s.sourceName} →</a></p><p><a class="read-more" href="../index.html">← Back to tracker</a></p>`;});
