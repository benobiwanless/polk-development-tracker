const params=new URLSearchParams(window.location.search);const id=params.get("id");
fetch("../data/projects.json").then(r=>r.json()).then(projects=>{const s=projects.find(x=>x.id===id)||projects[0];document.title=s.title+" | Polk Development Tracker";document.getElementById("projectDetail").innerHTML=`<p class="eyebrow">${s.category}</p><h1>${s.title}</h1><p class="meta">📍 ${s.city} • Status: ${s.status} • Phase: ${s.phase} • Updated ${s.updated}</p><p>${s.summary}</p><p>${s.details}</p><h2>Timeline</h2><ul class="timeline">${(s.timeline||[]).map(t=>`<li class="${t.complete?"done":""}"><strong>${t.label}</strong><br><span class="meta">${t.date}</span></li>`).join("")}</ul><p><a class="source" href="${s.sourceUrl}" target="_blank">Original source: ${s.sourceName} →</a></p><p><a class="read-more" href="../index.html">← Back to tracker</a></p>`;});
